<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Prevent all cleartext (HTTP) traffic to enforce HTTPS and zero unencrypted data leakage -->
    <base-config cleartextTrafficPermitted="false" />
</network-security-config>
